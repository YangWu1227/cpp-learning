#include <iostream>
#include <string>
using std::cout;
using std::endl;

int main()
{

    // Initialize
    int count = 0;

    // Condition
    while (count <= 7)
    {
        cout << count << '\n';
        // Increment
        count++;
    }

    return 0;
}